{
  "name": "primary-simplified",
  "version": "26.6.1",
  "private": true,
  "description": "Build tooling for Primary Simplified, an independently maintained fork of the Primary theme for Obsidian.",
  "author": "voitech",
  "license": "GPL-3.0",
  "scripts": {
    "build": "node scripts/build-theme.mjs",
    "watch": "node scripts/build-theme.mjs --watch",
    "test": "node --test"
  },
  "devDependencies": {
    "sass": "^1.89.2"
  }
}
